// Production backend URL (Railway). Override via localStorage "backend_base_url" if needed.
window.APP_CONFIG = window.APP_CONFIG || {
  backendBaseUrl: "https://backendis207-production.up.railway.app"
};
